# Hyperparameters and settings for deep learning model training
learning_rate: 0.00002
epochs: 3
batch_size: 8
optimizer: "AdamW"
weight_decay: 0.01
early_stopping_patience: 2
seed: 42
warmup_ratio: 0.1
max_grad_norm: 1.0
logging_steps: 10
eval_steps: 50
